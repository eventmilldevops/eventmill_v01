{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "validate_input"
      ],
      "default": "validate_input",
      "description": "Deterministic action. 'normalize_paths' and 'generate_detections' are planned (stages N4 and G1) and are rejected with a message naming them."
    },
    "artifact_ids": {
      "type": "array",
      "minItems": 1,
      "maxItems": 2,
      "items": {
        "type": "string"
      },
      "description": "Registered artifact ids for adversary_path_projector exports: one path graph, one scenario seed, or both documents of the same projection run. The usual route, and the only one that works when the export lives in cloud storage."
    },
    "artifact_id": {
      "type": "string",
      "description": "A single registered artifact id. Accepted because the shell speaks the singular; a pair needs artifact_ids."
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "maxItems": 2,
      "items": {
        "type": "string"
      },
      "description": "File paths to projector exports, as an alternative to artifact_ids. Local paths only."
    },
    "file_path": {
      "type": "string",
      "description": "Injected by the shell when a singular artifact_id is resolved; treated as a source path."
    },
    "path": {
      "type": "string",
      "description": "Injected by the shell alongside file_path; treated as a source path."
    },
    "accept_unverified_pair": {
      "type": "boolean",
      "default": false,
      "description": "Join two documents that carry no provenance block, on equal derived projection identity. The join is recorded as asserted_by_operator and never as verified."
    },
    "flow_map_artifact_id": {
      "type": "string",
      "description": "Registered artifact id of a JSON flow map. Its lineage against the export's flow_map_sha256 is recorded as same_map, edited_map or unhashed and is never grounds for refusal: an analyst's edited copy is an expected input."
    },
    "flow_map_path": {
      "type": "string",
      "description": "Local path to a JSON flow map, as an alternative to flow_map_artifact_id."
    }
  },
  "additionalProperties": false,
  "anyOf": [
    {
      "required": [
        "artifact_ids"
      ]
    },
    {
      "required": [
        "artifact_id"
      ]
    },
    {
      "required": [
        "sources"
      ]
    },
    {
      "required": [
        "file_path"
      ]
    }
  ]
}
