{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Threat Report Analyzer Output",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "description": "Action that was performed"
    },
    "reports": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "path": {"type": "string"},
          "name": {"type": "string"},
          "size_bytes": {"type": "integer"},
          "last_modified": {"type": "string"}
        },
        "required": ["path", "name"]
      },
      "description": "Available reports (for list_reports action)"
    },
    "summaries": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "report_path": {"type": "string"},
          "word_count": {"type": "integer"},
          "pages_total": {"type": "integer", "description": "Pages the source PDF has. Absent when native ingestion read the whole document, where coverage is total by construction."},
          "pages_read": {"type": "integer", "description": "Pages actually examined on the text path. Equal to pages_total unless the local page ceiling truncated the read."},
          "pages_dropped": {"type": "integer", "description": "pages_total - pages_read. Non-zero means the summary was built from part of the report, so a topic's absence is not evidence it is absent from the document."},
          "summary": {"type": "string"},
          "key_findings": {
            "type": "array",
            "items": {"type": "string"}
          },
          "relevant_techniques": {
            "type": "array",
            "items": {"type": "string"}
          }
        },
        "required": ["report_path", "summary"]
      },
      "description": "Generated summaries (for summarize action)"
    },
    "search_results": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "report_path": {"type": "string"},
          "matches": {"type": "array", "items": {"type": "string"}}
        },
        "required": ["report_path"]
      },
      "description": "Search results (for search_reports action)"
    },
    "error_code": {
      "type": "string",
      "description": "Error code if action failed"
    },
    "message": {
      "type": "string",
      "description": "Status or error message"
    }
  },
  "required": ["action", "message"]
}
